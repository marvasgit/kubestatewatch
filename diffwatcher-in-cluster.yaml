apiVersion: v1
kind: Pod
metadata:
  name: diffwatcher
  namespace: default
spec:
  containers:
  - image: tuna/diffwatcher:v0.0.1
    imagePullPolicy: Always
    name: diffwatcher
    volumeMounts:
    - name: config-volume
      mountPath: /root
  restartPolicy: Always
  volumes:
  - name: config-volume
    configMap:
      name: diffwatcher
